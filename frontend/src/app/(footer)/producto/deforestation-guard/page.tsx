import Page, { metadata as pageMetadata } from '@/app/(footer)/mrv/deforestation-guard/page'

export const metadata = pageMetadata

export default function DeforestationGuardPage() {
  return <Page />
}
