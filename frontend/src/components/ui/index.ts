/**
 * Shim de compatibilidad — @/components/ui
 * ─────────────────────────────────────────
 * Re-exporta todo desde el Design System oficial.
 *
 * ⚠️  Para nuevo código, usar directamente:
 *     import { ... } from '@/design-system'
 *
 * Este archivo existe para no romper imports anteriores.
 */
export * from '@/design-system';
