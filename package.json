{
  "scripts": {
    "deploy:staging": "git add . && git commit -m \"Despliegue a Staging\" && git push origin staging",
    "deploy:prod": "git checkout main && git pull origin main && git merge staging -m \"Pase a Produccion\" && git push origin main && git checkout staging"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
